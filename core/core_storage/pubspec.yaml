name: core_storage
description: Key-value persistence behind narrow interfaces — secure, persistent and in-memory.
publish_to: none
version: 1.0.0

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: '>=3.29.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  core_kit: ^1.0.0
  injectable: ^2.6.0
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.13
  injectable_generator: ^2.6.2
